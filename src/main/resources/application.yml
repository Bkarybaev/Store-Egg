spring:
  datasource:
    url: jdbc:postgresql://localhost:5454/eggdatabase
    username: postgres
    password: baiel123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9091
  address: 0.0.0.0

